# Redis 配置（可选）
# 如果不配置，系统会自动禁用缓存功能，不影响正常使用

# Redis 服务器地址
REDIS_ADDR=localhost:6379

# Redis 密码（如果有）
# REDIS_PASSWORD=your_password_here

# 说明：
# 1. 如果你有 Redis 服务器，配置后可以大幅提升 GeoIP 查询性能
# 2. 如果没有 Redis，系统会自动禁用缓存，仍然可以正常工作
# 3. 推荐使用 Docker 快速启动 Redis：
#    docker run -d --name redis -p 6379:6379 redis:alpine
