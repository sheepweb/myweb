════════════════════════════════════════════════════════════════
  直接复制下面的命令到SSH执行（推荐）
════════════════════════════════════════════════════════════════

cd /www/wwwroot/dy.moneyfly.top/frontend && echo "🔧 修复中..." && rm -rf dist node_modules/.cache node_modules/.vite .vite && npm cache clean --force && npm run build && nginx -s reload && echo "✅ 完成！清除浏览器缓存后访问测试"

════════════════════════════════════════════════════════════════
修复后：Ctrl+Shift+Delete 清除浏览器缓存 → 访问测试
════════════════════════════════════════════════════════════════
